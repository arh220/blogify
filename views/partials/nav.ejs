<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark" data-bs-theme=dark>
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Blogify</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>

                <% if (locals.user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/blog/add-blog">Add Blog</a>
                    </li>
                    <% } else {%>

                        <li class="nav-item">
                            <a class="nav-link" href="/user/reg">Create Account</a>
                        </li>
                        <% } %>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <% if (locals.user) { %>
                    <li class="nav-item dropdown text-center">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                            aria-expanded="false">
                            <img src="<%= user.profile %>" alt="abc" height="50px" width="50px"
                                style="border-radius: 50%;">
                            <br>
                            <%= user.email %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
                        </ul>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/user/login"><i class="fa-solid fa-user"></i><br>Login</a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>

<% if(locals.error) {%>
    <div class="container w-50 mt-5">
        <div class="alert alert-danger" role="alert">
            <%= locals.error %>
        </div>
    </div>
    <%}%>